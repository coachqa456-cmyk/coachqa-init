services:
  # PostgreSQL Database
  postgres:
    image: postgres:16-alpine
    container_name: qenabler-postgres
    environment:
      POSTGRES_DB: qenabler_db
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      # Set to "true" to use complete database setup (000-complete-database-setup.sql)
      # Set to "false" or leave empty to use individual migrations (001-010)
      USE_COMPLETE_SETUP: ${USE_COMPLETE_SETUP:-true}
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./qenabler-backend/migrations:/docker-entrypoint-initdb.d
    networks:
      - qenabler-network
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 10s
      timeout: 5s
      retries: 5

  # Backend API Service
  backend:
    build:
      context: ./qenabler-backend
      dockerfile: Dockerfile
    container_name: qenabler-backend
    environment:
      NODE_ENV: production
      PORT: 9002
      DB_HOST: postgres
      DB_PORT: 5432
      DB_NAME: qenabler_db
      DB_USER: postgres
      DB_PASSWORD: postgres
      JWT_SECRET: ${JWT_SECRET:-your-secret-key-change-in-production}
      JWT_REFRESH_SECRET: ${JWT_REFRESH_SECRET:-your-refresh-secret-key-change-in-production}
      # Add other environment variables as needed
      EMAIL_HOST: ${EMAIL_HOST}
      EMAIL_PORT: ${EMAIL_PORT}
      EMAIL_USER: ${EMAIL_USER}
      EMAIL_PASSWORD: ${EMAIL_PASSWORD}
    ports:
      - "9002:9002"
    volumes:
      - ./qenabler-backend/logs:/app/logs
      - ./qenabler-backend/ensure-database.js:/app/ensure-database.js
    depends_on:
      postgres:
        condition: service_healthy
    networks:
      - qenabler-network
    restart: unless-stopped
    command: sh -c "node ensure-database.js && npm start"

  # Frontend UI Service
  frontend:
    build:
      context: ./qenabler-ui
      dockerfile: Dockerfile
    container_name: qenabler-frontend
    environment:
      VITE_API_URL: http://127.0.0.1:9002
    ports:
      - "9001:80"
    depends_on:
      - backend
    networks:
      - qenabler-network
    restart: unless-stopped

  # Swagger UI Service - Port 9004
  swagger-ui:
    build:
      context: ./qenabler-swagger-ui
      dockerfile: Dockerfile
    container_name: qenabler-swagger-ui
    environment:
      PORT: 9004
      BACKEND_URL: http://backend:9002
      BACKEND_URL_EXTERNAL: http://127.0.0.1:9002
    ports:
      - "9004:9004"
    depends_on:
      - backend
    networks:
      - qenabler-network
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "node", "-e", "require('http').get('http://localhost:9004/health', (r) => {process.exit(r.statusCode === 200 ? 0 : 1)})"]
      interval: 30s
      timeout: 3s
      retries: 3
      start_period: 10s

  # Documentation Service (Docusaurus) - Port 9003
  docs:
    build:
      context: ./qenabler-docs
      dockerfile: Dockerfile
    container_name: qenabler-docs
    ports:
      - "9003:80"
    networks:
      - qenabler-network
    restart: unless-stopped

volumes:
  postgres_data:
    driver: local

networks:
  qenabler-network:
    driver: bridge

